package org.fatec.appfatechub;

/**

 * @author maykon
 */

public class AppFatecHub {

    
    // variável que guarda o valor atual do contador
    // private static int contador = 0;
    
    public static void main(String[] args) {

    //     Javalin app = Javalin.create(config -> {
    //         // libera qualquer origem chamar essa API (CORS) -- útil em desenvolvimento local
    //         config.bundledPlugins.enableCors(cors -> {
    //             cors.addRule(it -> it.anyHost());
    //         });
    //     }).start(7000);

    //     // rota GET simples: devolve o valor atual do contador sem alterar
    //     app.get("/api/contador", ctx -> {
    //         ctx.json(new ContadorResponse(contador));
    //     });

    //     // rota POST: incrementa o contador em 1 e devolve o novo valor
    //     app.post("/api/contador/
    //     incrementar", ctx -> {
    //         contador = contador + 1;
    //         ctx.json(new ContadorResponse(contador));
    //     });

    //     System.out.println("Servidor rodando em http://localhost:7000");
    // }

    // // classe simples só pra representar a resposta em JSON: { "valor": 5 }
    // static class ContadorResponse {
    //     public int valor;

    //     public ContadorResponse(int valor) {
    //         this.valor = valor;
    //     }
    }

}
